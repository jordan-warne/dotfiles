- hosts: localhost
  gather_facts: false
  become: true
  tasks:
    - name: Install packages
      command: pkg_add {{ item }}
      loop:
        - vim--no_x11
        - curl
        - wget
        - firefox
        - git
        - cmus
        - ranger
        - cmixer
        - obsdfreqd
        - unzip--
        - rsync--
        - dina-fonts
        - picom
        - profanity
        - newsboat
        - mpv
        - yt-dlp
